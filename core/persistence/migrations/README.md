# Persistence Migrations

Alembic owns schema changes for the Agent Support control plane.

Use the admin database URL for migrations. Application code must use the app role so RLS tests are meaningful.
